// Registration disabled for v1 website
export function register() {
  console.info('Service Worker registration deferred to Vite PWA phase');
}

export function unregister() {
  console.info('Service Worker registration deferred to Vite PWA phase');
}